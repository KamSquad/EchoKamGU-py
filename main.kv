<MainScreenManager@ScreenManager>
    transition: NoTransition()

    Screen:
        name: "greetings"
        GreetingsScreen:
            main_manager: root

    Screen:
        name: "news"
        MainBackdrop:
            main_manager: root

    Screen:
        name: "registration"
        RegistrationScreen:
            main_manager: root
